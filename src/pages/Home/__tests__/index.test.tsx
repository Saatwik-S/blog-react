import { render, screen, waitFor } from '@testing-library/react'
import Home from '..'
import React from 'react'
import Header from '../../../components/Header'

describe('Home Page', () => {
  // it('should check if all card components are there are not', async () => {
  //   const { asFragment } = render(<Home />)
  //   expect(screen.getAllByTestId('box')
  //   ).toHaveLength(6)
  //   await waitFor(() => {
  //     screen.getByText('How painting outdoors can transform your ')
  //   })
  //   expect(asFragment).toMatchSnapshot()
  // })
})
